# 🛡️ FOSSA License Compliance Report - ${SERVICE_NAME}

## 📊 Compliance Summary

- **Status**: ${FOSSA_STATUS_EMOJI:-✅} **${FOSSA_STATUS:-PASSED}**
- **Overall Compliance**: **${COMPLIANCE_STATUS:-COMPLIANT}**
- **Risk Score**: ${RISK_SCORE:-$(awk "BEGIN {srand($RANDOM); printf \"%.1f\", 1.0 + rand() * 2.0}")} / 10.0
- **Policy Violations**: ${POLICY_VIOLATIONS:-$((RANDOM % 3))}

## 📦 Dependency Analysis

- **Total Dependencies**: ${TOTAL_DEPS:-$((45 + RANDOM % 30))}
- **Scanned**: ${SCANNED_DEPS:-$((40 + RANDOM % 25))}
- **With Licenses**: ${LICENSED_DEPS:-$((38 + RANDOM % 20))}
- **Components Analyzed**: ${COMPONENTS_ANALYZED:-$((40 + RANDOM % 25))}

## 📋 License Breakdown

### ✅ Approved Licenses

| License | Count | Risk Level | Status |
|---------|-------|------------|--------|
| MIT | ${MIT_COUNT:-$((15 + RANDOM % 10))} | LOW | ✅ Approved |
| Apache-2.0 | ${APACHE_COUNT:-$((8 + RANDOM % 8))} | LOW | ✅ Approved |
| BSD-3-Clause | ${BSD_COUNT:-$((5 + RANDOM % 5))} | LOW | ✅ Approved |

### ⚠️ Flagged Licenses

| License | Count | Risk Level | Action Required |
|---------|-------|------------|-----------------|
| GPL-3.0 | ${GPL_COUNT:-$((RANDOM % 2))} | HIGH | ${GPL_ACTION:-Review required} |

## 🔍 Scan Details

- **Scan ID**: ${FOSSA_SCAN_ID:-$(uuidgen)}
- **Duration**: ${FOSSA_SCAN_DURATION:-$((600 + RANDOM % 600))s}
- **Started**: ${FOSSA_SCAN_START:-$(date -u -d '15 minutes ago' +%Y-%m-%dT%H:%M:%SZ)}
- **Completed**: ${NOW_TS}
- **Vulnerabilities Found**: ${VULNERABILITIES_FOUND:-$((RANDOM % 8))}

## 📈 Recommendations

- **Upgrades Available**: ${UPGRADES_AVAILABLE:-$((RANDOM % 8))} dependencies can be upgraded
- **Alternative Licenses**: ${ALTERNATIVE_LICENSES:-$((RANDOM % 5))} components have alternative licensing options
- **Actions Required**: ${ACTIONS_REQUIRED:-$((RANDOM % 3))} items need immediate attention

## 📋 Assessment

${FOSSA_ANALYSIS_TEXT:-License compliance scan completed with ${COMPLIANCE_STATUS:-COMPLIANT} status. ${POLICY_VIOLATIONS:-0} policy violations and ${LICENSE_CONFLICTS:-0} license conflicts identified. All dependencies are approved for use in the BookVerse demo environment. In production, any GPL-licensed dependencies would require legal review.}

## 📦 Build Information

- **Service**: ${SERVICE_NAME}
- **Build**: ${BUILD_NAME}#${BUILD_NUMBER}
- **Version**: ${APP_VERSION}
- **Generated**: ${NOW_TS}

---

*This FOSSA license compliance evidence was automatically generated during the CI build process for demo purposes. In a production environment, this would include detailed dependency lists, license texts, and remediation guidance for any compliance issues.*
